Introduce realtime UI notifications (signalR/socket.io)

Add common lib for message types

Improve unit tests coverage	

Cleanup codebase: external API to become a microservice
	project paths, folder structure and namespaces will affect 'add_command.js'

	simulators
		agent (console app)
		agent-runner	
