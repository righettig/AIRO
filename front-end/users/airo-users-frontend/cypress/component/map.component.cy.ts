import { MapComponent } from '../../src/app/home/components/events/components/event-live-feed/map/map.component'

describe('MapComponent', () => {
  it('should mount', () => {
    cy.mount(MapComponent)
  })
})