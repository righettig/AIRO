import { ProgressBarComponent } from '../../src/app/common/components/progress-bar/progress-bar.component'

describe('ProgressBarComponent', () => {
  it('should mount', () => {
    cy.mount(ProgressBarComponent)
  })
})